{
  "name": "front-end-fisheye",
  "main": "index.js",
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-strongloop": "^2.1.0"
  },
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore ."
  }
}